@page
@model BatteryPeykCustomers.Pages.Admin.Ampers.EditModel

@{
    ViewData["Title"] = "Edit";
}

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Amper.Id" />
            <div class="form-group mb-2">
                عنوان
                <input asp-for="Amper.Title" class="form-control" />
                <span asp-validation-for="Amper.Title" class="text-danger"></span>
            </div>
            <div class="form-group mb-2">
                آمپر
                <input asp-for="Amper.Amperage" class="form-control" />
                <span asp-validation-for="Amper.Amperage" class="text-danger"></span>
            </div>
            <div class="form-group mb-2">
                <input type="submit" value="ذخیره" class="btn btn-info mt-2" />
                <a asp-page="./Index" class="btn btn-success mt-2">بازگشت</a>

            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
