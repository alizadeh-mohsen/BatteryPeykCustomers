@page
@using BatteryPeykCustomers.Helpers
@model BatteryPeykCars.Pages.Admin.Cars.DetailsModel

@{
    ViewData["Title"] = "Details";
}
<script type="text/javascript">
    function copy(phone) {
        navigator.clipboard.writeText("http://BatteryPeyk.com/vip.html?m=" + phone);
    }
</script>

<p>
</p>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.car.Make</h5>
        <h6 class="card-subtitle mb-2 text-body-secondary">
            @Model.car.Battery
        </h6>
        <p class="card-text">
            <table class="table">
                <tr>
                    <td><strong>تاریخ خرید</strong></td>
                    <td>
                        @Model.car.PurchaseDate.ToPersianDate()
                    </td>
                </tr>
                <tr>
                    <td><strong>گارانتی</strong></td>
                    <td>@Model.car.Guaranty</td>
                </tr>
                <tr>
                    <td><strong>طول عمر</strong></td>
                    <td>@Model.car.LifeExpectancy</td>
                </tr>
                <tr>
                    <td><strong>زمان تعویض</strong></td>
                    <td>
                        @Model.car.ReplaceDate.ToPersianDate()
                    </td>
                </tr>

                <tr>
                    <td><strong>توضیحات</strong></td>
                    <td>@Model.car.Comments</td>
                </tr>
            </table>

        </p>
        <a class="btn btn-info ml-2" asp-page="Edit" asp-route-id="@Model.car.Id">ویرایش</a>
        <a class="btn btn-success ml-2" href='javascript:history.go(-1)'>بازگشت</a>

    </div>
</div>

