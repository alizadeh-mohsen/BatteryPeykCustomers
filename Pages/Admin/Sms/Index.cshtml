@page
@model BatteryPeykCustomers.Pages.Admin.Sms.IndexModel
@{
}


<div class="row text-center">
    <div class="col-sm-4 mx-3">
        <form method="post">
            <button class="btn btn-primary m-2" type="submit" asp-page-handler="SendWelcomeMessage">تست پیامک عضویت</button>
            <button class="btn btn-primary m-2" type="submit" asp-page-handler="SendUpdateMessage">تست پیامک تعویض باتری</button>
        </form>
    </div>
</div>

<div class="mt-3">
@if (!string.IsNullOrEmpty(Model.Message))
{
    <p><strong>@Model.Message</strong></p>
}
</div>


